%h1 Create New Product

= simple_form_for @product do |f|
  = f.input :title
  = f.input :price
  = f.input :description
  = f.input :category_id, collection: categories_options(@categories)
  = f.button :submit
